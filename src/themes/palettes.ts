// Palettes are now owned by each skin as variants — see skins.ts
// This file is kept as a stub so any stale import doesn't hard-error.
export const palettes: never[] = []
export function getPalette(_id: string): never { throw new Error('getPalette removed — use skin.variants') }
